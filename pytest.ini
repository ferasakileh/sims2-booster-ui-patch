[pytest]
# This project’s tests are written for unittest and often use absolute imports like
# `from tests.test_base import BaseTestCase`. Pytest’s default import mode can break
# those imports, so force a sys.path-based import mode.
addopts = --import-mode=prepend

testpaths = tests
norecursedirs = submodules .venv
